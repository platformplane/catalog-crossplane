apiVersion: catalog.cluster.local/v1
kind: MyTest
metadata:
  name: my-test-example
spec:
#  parameters:
    # Storage size for the resource (default: "8Gi")
    size: "16Gi"

    # Ingress configuration
    ingress:
      # Whether to expose a public endpoint (default: true)
      # Warning: Setting this to true makes your application publicly accessible on the internet
      public: false
